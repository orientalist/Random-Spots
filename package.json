{
  "name": "randomVenue",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css":"node-sass src -o src",
    "css:watch":"npm run css && node-sass src -wo src",
    "build":"cp index.html ./build && cp -r ./src ./build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "node-sass": "^7.0.0"
  }
}
